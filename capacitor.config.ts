import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jeep.app.react.datastoragesqlite',
  appName: 'react-datastoragesqlite-app',
  webDir: 'build',
  bundledWebRuntime: false
};
export default config;
